const initialState = [
    { id: 1, body: "som request for aadhar card", iscompleted: false },
    { id: 2, body: "raise request for passport renewal", iscompleted: false }
];



function RootReducer(state=initialState, action) {
    
    return state;
}
export default RootReducer;
